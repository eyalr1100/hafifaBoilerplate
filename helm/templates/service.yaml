{{- $releaseName := .Release.Name -}}
{{- $chartName := include "eyal-hafifa-server.name" . -}}
{{- $cloudProviderFlavor := include "eyal-hafifa-server.cloudProviderFlavor" . -}}
{{- if .Values.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ $releaseName }}-{{ $chartName }}
  labels:
    app: {{ $chartName }}
    component: {{ $chartName }}
    environment: {{ include "eyal-hafifa-server.environment" . }}
    release: {{ $releaseName }}
    {{- include "eyal-hafifa-server.labels" . | nindent 4 }}
spec:
  {{- if eq $cloudProviderFlavor "minikube" }}
  type: NodePort
  {{- end }}
  ports:
    - port: {{ .Values.env.port }}
      targetPort: {{ .Values.env.targetPort }}
      protocol: {{ .Values.env.protocol }}
      name: http
  selector:
    app: {{ $chartName }}
    release: {{ $releaseName }}
    run: {{ $releaseName }}-{{ $chartName }}
    {{- include "eyal-hafifa-server.selectorLabels" . | nindent 4 }}
{{- end }}
